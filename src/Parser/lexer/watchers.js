import PREFIX from "./prefix.js";

// A watcher need to track data changes
const WATCHERS = {
  "bind":     PREFIX + "bind",
  "for":      PREFIX + "for",
  "if":       PREFIX + "if"
};

export default WATCHERS;
