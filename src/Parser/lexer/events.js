import PREFIX from "./prefix.js";

const EVENTS = {
  "click":      PREFIX + "click",
  "mouseOver":  PREFIX + "mouseover",
  "keyUp":      PREFIX + "keyup",
  "input":      PREFIX + "input"
};

export default EVENTS;
